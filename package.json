{
  "name": "kanban-project-smoke",
  "version": "0.1.0",
  "description": "Smoke test harness for Kanban project",
  "scripts": {
    "smoke": "node scripts/smoke-test.js",
    "interaction": "node scripts/interaction-test.js",
  "interaction:playwright": "node scripts/playwright-interaction-test.js",
  "ci:test": "node scripts/playwright-interaction-test.js",
  "test:db": "npx playwright test tests/db-where.spec.js",
  "test": "npx playwright test",
    "lint:css": "stylelint \"css/**/*.css\" --fix"
  },
  "devDependencies": {
    "puppeteer": "^21.0.0",
    "playwright": "^1.40.0",
    "stylelint": "^15.0.0",
    "stylelint-config-standard": "^33.0.0"
  }
}
